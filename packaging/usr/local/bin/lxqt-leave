#!/bin/bash
# Prevent infinite recursion if lxqt-leave calls itself
if [ -z "$JIOPC_SAVE_TRIGGERED" ]; then
    export JIOPC_SAVE_TRIGGERED=1
    cd /usr/lib/jiopc-hibernate
    python3 -m save.save_service || true
fi
exec /usr/bin/lxqt-leave "$@"
