This .desktop file is installed to /etc/xdg/autostart/ (or ~/.config/autostart/) to automatically execute the restore service in the background whenever a user logs into the LXQt desktop environment.
